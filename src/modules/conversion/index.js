'use strict';

// Barrel export for the conversion module.
module.exports = {
  routes: require('./conversion.routes'),
  service: require('./conversion.service'),
  controller: require('./conversion.controller'),
};
